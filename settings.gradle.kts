pluginManagement {
    repositories {
        maven {
            url = uri("https://reposilite.beyond.tain.one/releases")
        }
//        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "cf-ddns"
